let express,readdirSync,cors,mongoose,morgan,dotenv;_6db‍.w("express",[["default",["express"],function(v){express=v}]]);_6db‍.w("fs",[["readdirSync",["readdirSync"],function(v){readdirSync=v}]]);_6db‍.w("cors",[["default",["cors"],function(v){cors=v}]]);_6db‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);_6db‍.w("morgan",[["default",["morgan"],function(v){morgan=v}]]);_6db‍.w("dotenv",[["default",["dotenv"],function(v){dotenv=v}]]);



  // Use ES6 import for morgan as well
  // Use ES6 import for dotenv

dotenv.config();  // Load environment variables
_6db‍.g.console.log("JWT_SECRET:", process.env.JWT_SECRET);

const app = express();

// db connection
mongoose
  .connect(process.env.DATABASE, {
    useNewUrlParser: true,
    useFindAndModify: false,
    useUnifiedTopology: true,
    useCreateIndex: true,
  })
  .then(() => console.log("DB Connected"))
  .catch((err) => _6db‍.g.console.log("DB Connection Error: ", err));

// middlewares
app.use(cors());
app.use(morgan("dev"));
app.use(express.json());

// route middleware
readdirSync("./routes").map((r) => app.use("/api", require(`./routes/${r}`)));

const port = process.env.PORT || 8000;

app.listen(port, () => console.log(`Server is running on port ${port}`));

